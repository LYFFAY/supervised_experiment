# supervised_experiment
Supervised learning Train KNN, decision tree, and naive Bayes separately and adjust parameters. Then conduct ensemble learning. The best single model is standardization + KNN (5), and the best ensemble model is standardization + hard voting. The classification accuracy is the highest.
